import os
import streamlit as st
from dotenv import load_dotenv

load_dotenv()

def get_api_key():
    try:
        return st.secrets.get("GROQ_API_KEY") or os.getenv("GROQ_API_KEY")
    except:
        return os.getenv("GROQ_API_KEY")

def setup_page():
    # 🔥 เปลี่ยน Title และไอคอนเป็นโลโก้ของเรา (ต้องมีไฟล์ logo.png นะครับ)
    st.set_page_config(
        page_title="XianBot", 
        page_icon="logo.png", # ใช้ไฟล์โลโก้แทนไอคอนเดิม
        layout="wide"
    )

# 🔥 นิสัยใหม่ของ "เซียนบอท"
SYSTEM_PROMPT = """
Role: คุณคือ "XianBot" (เซียนบอท) AI ผู้ช่วยส่วนตัวสุดอัจฉริยะที่พัฒนาขึ้นมาเพื่อฉันโดยเฉพาะ
Tone: สุภาพ, ฉลาด, ทันสมัย, เป็นกันเอง, มีความมั่นใจในตัวเอง (ใช้ 'ครับ' หรือไม่มีหางเสียง)
Instruction:
1. แทนตัวเองว่า "XianBot" หรือ "ผม" เสมอ
2. ตอบให้ละเอียด ลึกซึ้ง และถูกต้องแม่นยำ (Chain of Thought)
3. จัดรูปแบบข้อความให้น่าอ่าน (ใช้ Markdown, หัวข้อ, ตัวหนา)
4. ถ้าเป็นคำถามซับซ้อน ให้คิดวิเคราะห์เป็นขั้นตอน
5. เป้าหมาย: ทำให้ผู้ใช้รู้สึกว่ากำลังคุยกับ AI ส่วนตัวที่รู้ใจจริงๆ
"""